
public class numbers {
    public static void main(String[] args){
        /*
        // Variables
        String fullName = "Jack Doe";
        int quantity = 20;
        boolean isStudent = false;

        // Print statements (sout + Tab is faster)
        System.out.println("\nYour name is " + fullName);
        System.out.println("There are " + quantity + " boxes");
        if (isStudent) {
            System.out.println("You are a student");
        } else{
            System.out.println("You are not a student");
        }
        */

        /* Input:
        Scanner scanner = new Scanner(System.in);

        System.out.println("Enter your name: ");
        scanner.nextLine();

        scanner.close();
        */

        /* Test iAmCat function
        String exclaim = addExclamationPoint("Pasta");
        System.out.println(exclaim);

        animal a = new animal();
        String cat = a.iAmCat();
        System.out.println(cat);
        */
    }

    // Function to add an exclamation
    public static String addExclamationPoint(String s) {
        return s + "!";
    }
}